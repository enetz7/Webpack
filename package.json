{
  "name": "Leaflet-Webpack",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.20",
    "webpack-cli": "^4.2.0"
  },
  "scripts": {
    "prebuild": "rm -r dist",
    "build": "webpack --mode=production",
    "start": "yarn prebuild && webpack --mode=development"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "sass": "^1.29.0",
    "sass-loader": "^10.1.0",
    "webpack": "^5.6.0"
  }
}
